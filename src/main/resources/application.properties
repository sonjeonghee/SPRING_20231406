spring.application.name=demo
spring.web.resources.static-locations=classpath:/static/

# --- Development: use in-memory H2 to avoid needing a running MySQL server ---
# Original MySQL settings (commented out)

# H2 in-memory datasource for local development
## Using H2 for local development to avoid requiring MySQL
# MySQL settings (commented out)
# spring.datasource.url=jdbc:mysql://localhost:3306/spring?serverTimezone=Asia/Seoul
# spring.datasource.username=yjj
# spring.datasource.password=wpwjd2ek
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# H2 in-memory DB (for development/testing)
spring.datasource.url=jdbc:h2:mem:demo;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.datasource.url=jdbc:h2:mem:demo;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Enable H2 console for quick DB inspection (http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Misc
spring.mvc.hiddenmethod.filter.enabled=true
server.servlet.session.timeout=300s
server.servlet.session.cookie.secure=true
spring.servlet.multipart.enabled=true
spring.servlet.multipart.location=./SPRING_20211000/src/main/resources/static/upload/
spring.servlet.multipart.max-request-size=30MB
spring.servlet.multipart.max-file-size=10MB